{
  "name": "vite-project",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "serve": "npx http-server -p 8000 server/public"
  },
  "devDependencies": {
    "vite": "^2.0.0-beta.12"
  }
}